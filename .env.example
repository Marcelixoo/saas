# Server Configuration
SERVER_PORT=8080

# Database Configuration
# For persistent storage, use: /app/data/articles.db
# For in-memory (testing), use: file:articles.db?cache=shared&mode=memory
DATABASE_PATH=/app/data/articles.db

# JWT Authentication (REQUIRED)
SERVER_PORT=8081

# Database Configuration
DATABASE_PATH=file:articles.db?cache=shared&mode=memory

# Meilisearch Configuration
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=  # Optional - required for production Meilisearch instances

# JWT Configuration (REQUIRED)
JWT_SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_ISSUER=fashion-catalog
JWT_ACCESS_TTL=24h
JWT_REFRESH_TTL=168h

# Search API Rate Limiting
# Maximum number of search requests allowed per minute per IP address
# Default: 60 requests/minute if not set
SEARCH_RATE_LIMIT=60

# GCP Configuration (for deployment)
GCP_SA_KEY=
GCP_PROJECT_ID=criticalmars-saas
