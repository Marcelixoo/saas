# Placeholder dev secret. DO NOT use these values outside local development.
# Overlays (local/gke) MUST replace these values via a secretGenerator or
# patch before applying to any shared/real environment.
apiVersion: v1
kind: Secret
metadata:
  name: saas-secrets
  namespace: saas
type: Opaque
stringData:
  POSTGRES_USER: "saas"
  POSTGRES_PASSWORD: "saas-dev-password"
  DATABASE_URL: "postgresql://saas:saas-dev-password@postgres:5432/saas"
  JWT_SECRET: "dev-only-change-me"
  JWT_SECRET_KEY: "dev-only-change-me"
  MEILISEARCH_API_KEY: "dev-meili-master-key-change-me"
